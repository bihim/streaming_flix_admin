import 'package:flutter/material.dart';

class ListVideoScreenWeb extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('I am List Screen'),
      ),
    );
  }
}
