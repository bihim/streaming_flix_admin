import 'package:flutter/material.dart';

class SearchVideoScreenWeb extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('I am Search Screen'),
      ),
    );
  }
}
